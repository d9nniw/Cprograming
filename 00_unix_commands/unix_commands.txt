1.mkdir<directory_name>(make directory). Creates a fold

2. cd<directory_name> (change directory). Changes the cuurent directory
to the specified one.

3. code <file_name>. Creates a file and opens it in a text editor. 

4. ls (list). Shows the content of the current directory. 

5. pwd (print working directory). Shows the path of the current directory.

6. clear - clears the temrinal window. 

7. gcc <fie_name (c sourse code)>- comple the program.

8. ./<executable_file>- runs the program. 

9. \-escape character 

\* - escape sequence